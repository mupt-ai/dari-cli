# {{.ProjectName}}

Scaffolded with `dari init`. Validate and deploy from this directory:

```bash
dari deploy --dry-run
dari deploy
```

## Layout

- `dari.yml` — manifest
- `prompts/system.md` — system prompt
- `tools/repo_search.ts` — code-first example tool; `dari deploy` generates the manifest/schema files in the uploaded bundle
- `skills/{{.SkillName}}/SKILL.md` — example skill
