# {{.ProjectName}}

Scaffolded with `dari init`. Validate and deploy from this directory:

```bash
dari deploy --dry-run
dari deploy
```

This scaffold uses Dari-managed OpenRouter and E2B keys by default. Upload credentials only when you add explicit BYOK fields such as `llm.api_key_secret`, `sandbox.provider_api_key_secret`, or names under `sandbox.secrets`.

## Layout

- `dari.yml` — manifest
- `prompts/system.md` — system prompt
- `tools/repo_search/` — example tool
- `skills/{{.SkillName}}/SKILL.md` — example skill
