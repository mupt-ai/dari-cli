from agents import Agent

agent = Agent(
    name="hello-openai-agents-python",
    instructions=(
        "You are a minimal OpenAI Agents SDK example used to verify "
        "Dari manifest validation and deploy packaging."
    ),
)
