# Hello OpenCode Example

This example exists to exercise `dari manifest validate` and `dari deploy`.

Run:

```bash
uv run dari manifest validate examples/hello-opencode --json
uv run dari deploy examples/hello-opencode --dry-run
```
