name: hello-pi
harness: pi

instructions:
  system: prompts/system.md

runtime:
  dockerfile: Dockerfile

tools:
  - name: repo_search
    path: tools/repo_search
    kind: main
  - name: sandbox.exec
    kind: ephemeral

secrets:
  - ANTHROPIC_API_KEY
  - OPENAI_API_KEY

env:
  APP_ENV: example
